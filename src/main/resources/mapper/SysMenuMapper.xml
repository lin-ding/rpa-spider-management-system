<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.rpa.mapper.SysMenuMapper">

    <resultMap id="SysMenuResult" type="com.example.rpa.entity.SysMenu">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="menuType" column="menu_type"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="permission" column="permission"/>
        <result property="icon" column="icon"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="visible" column="visible"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 根据用户 ID 查询菜单列表 -->
    <select id="selectMenusByUserId" resultMap="SysMenuResult">
        SELECT DISTINCT m.* 
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} 
          AND m.deleted = 0 
          AND rm.deleted = 0 
          AND ur.deleted = 0
        ORDER BY m.sort_order
    </select>

    <!-- 根据角色 ID 查询菜单列表 -->
    <select id="selectMenusByRoleId" resultMap="SysMenuResult">
        SELECT m.* 
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId} 
          AND m.deleted = 0 
          AND rm.deleted = 0
        ORDER BY m.sort_order
    </select>

</mapper>
